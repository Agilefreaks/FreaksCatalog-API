query Freaks($allOf: [ID!], $anyOf:[ID!], $last: Int, $before: String) {
    freaks(filter: {projectIds: {allOf: $allOf, anyOf: $anyOf}, technologyIds: {allOf: $allOf, anyOf: $anyOf}}, last: $last, before: $before) {
        edges {
            node {
                firstName
            }
        }
    }
}
